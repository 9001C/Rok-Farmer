import pyautogui
import time
import keyboard
import mouse 
import cv2
#import argparser thing

#argparse arguments for type

#DON'T RUN 24/7
BasePath = "" # file path to the image folder

RandomClickRange = 1 # amount of variance allowed in each click to avoid bot detection  
RandomTimeRange = 1 #Time variance to avoid bot detection 



def clearScreen:
  #search for x
  #check if in city
  
def RandomAction:
  #random action to simulate a real human playing 

def farm(type):
  #check if in or out of city then push city button
  #click search button
  if(type == 1):
    #search for food
  elif(type == 2):
    #search for wood
  elif(type == 3):
    #search for stone 
  elif(type == 4):
    #search for gold
    else:
      print(type + " not found")
  
    
